"""Finance MCP — financial tools for Hermes Agent."""
# Keep in step with `version` in pyproject.toml — tests/test_version.py fails
# the build if the two drift (they did: 0.2.0 here vs 0.3.0 released).
__version__ = "0.3.0"
